return {
  "scottmckendry/cyberdream.nvim",
  lazy = false,
  priority = 1000,
  config = function()
    -- require("cyberdream").setup({
    --   variant = "auto", -- or "light", etc.
    --   -- transparent = true,
    --   terminal_colors = true,
    --   cache = true, -- Improves start up time
    -- })
  end,
}
